export enum TimerState {
  pomodoro = "pomodoro",
  shortBreak = "shortBreak",
  longBreak = "longBreak",
}

export enum PomodoroCommandsNames {
  start = "start",
  pause = "pause",
  resume = "resume",
  reset = "reset",
}
